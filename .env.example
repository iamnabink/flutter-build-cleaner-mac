# Copy this file to `.env` before building:
#   cp .env.example .env
#
# RevenueCat public Apple SDK key (https://app.revenuecat.com → Project → API keys).
# Leave empty to run without in-app purchases — the Pro/paywall UI is hidden
# and everything else works normally.
REVENUECAT_API_KEY=
